{% if allowNull == true %}
    if (null !== {{ getProperty }} && false === {{ getProperty }}) {
{% else %}
    if (false === {{ getProperty }}) {
{% endif %}
        $violationsList[] = new \Kaa\Component\Validator\Violation('{{ class }}', '{{ property }}', '{{ message }}');
    }
