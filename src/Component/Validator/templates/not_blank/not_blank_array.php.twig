{% if allowNull == true %}
    if (null !== {{ getProperty }} && empty({{ getProperty }})) {
{% else %}
    if (empty({{ getProperty }})) {
{% endif %}
        $violationsList[] = new \Kaa\Component\Validator\Violation('{{ class }}', '{{ property }}', '{{ message }}');
    }
