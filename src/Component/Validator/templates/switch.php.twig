$violationsList = [];
switch(get_class($model)) {
{% for class, value in methodNames %}
    case \{{ class }}::class:
        $violationsList = $this->{{ value }}($model);

    break;
{% endfor %}
}
return $violationsList;