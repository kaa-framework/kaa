$before = \DateTime::createFromFormat('{{ format }}', '{{ before }}');
$date = \DateTime::createFromFormat('{{ format }}', {{ getProperty }});

if ($before < $date) {
    $violationsList[] = new \Kaa\Component\Validator\Violation('{{ class }}', '{{ property }}', '{{ message }}');
}
