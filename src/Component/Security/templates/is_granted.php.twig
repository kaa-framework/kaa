{% for attribute, voter in voters %}
    if ($attribute === '{{ attribute }}') {
        return ({{ instanceProvider->generate(voter.service, voter.serviceClass ?? voter.service) }})->vote($subject);
    }}


{% endfor %}
return false;
