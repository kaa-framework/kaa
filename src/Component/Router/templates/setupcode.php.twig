$route = $request->getPathInfo();
$method = $request->getMethod();
$nodes = explode("/", $route);
if ($nodes[0] == "") {
    unset($nodes[0]);
    $nodes = array_values($nodes);
}
$count_nodes = count($nodes);
