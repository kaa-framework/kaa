{% if not current.isVariable() %}
    {% if indexes[depth] is same as(0) %}
        if (($nodes[{{ depth }}] === '{{ route }}') && ($count_nodes >= {{ depth + 1 }})){
    {% else %}
        elseif (($nodes[{{ depth }}] === '{{ route }}') && ($count_nodes >= {{ depth + 1 }})){
    {%  endif %}

{% else %}
    {% if indexes[depth] is same as(0) %}
        if ($count_nodes >= {{ depth }}){
    {% else %}
        elseif ($count_nodes >= {{ depth }}){
    {%  endif %}

{% endif %}
